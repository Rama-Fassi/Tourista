import 'package:flutter/material.dart';

class HotelDetailViewBody extends StatelessWidget {
  const HotelDetailViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          color: Colors.white,
          child: Text('ffesfs'),
        ),
      ],
    );
  }
}
