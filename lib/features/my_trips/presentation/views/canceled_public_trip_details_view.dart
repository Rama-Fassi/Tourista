import 'package:flutter/material.dart';

class CanceledPublicTripDetailsView extends StatelessWidget {
  const CanceledPublicTripDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Scaffold());
  }
}
