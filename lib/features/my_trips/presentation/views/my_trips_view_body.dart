import 'package:flutter/widgets.dart';

class MyTripsViewBody extends StatelessWidget {
  const MyTripsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('my trips'));
  }
}
