import 'package:flutter/material.dart';

class ReadyTripMainViewBody extends StatelessWidget {
  const ReadyTripMainViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('ready trip'));
  }
}
